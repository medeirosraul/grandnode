@model ProductDetailsModel
@{
    var addtocomparelink = Url.RouteUrl("AddProductToCompare", new { productId = Model.Id });
}
@if (Model.CompareProductsEnabled)
{
    <b-button variant="secondary" class="add-to-compare-list-button mr-1" @@click="AxiosCart.addproducttocomparelist('@addtocomparelink');return false;" title="@T("Products.Compare.AddToCompareList")">
        <b-icon icon="shuffle"></b-icon>
        <span class="sr-only">@T("Products.Compare.AddToCompareList")</span>
    </b-button>
}
